# Externally-published modules folded into this catalog's index.json.
#
# Most modules in this catalog are built HERE: they are git submodules
# under submodules/, and the "Release <module>" workflows build, sign and
# publish their .lgx into this repo's own releases.
#
# Some can't be. The release pipeline builds one module per submodule
# ROOT (it reads <module_path>/metadata.json and cannot target a
# subdirectory), so a monorepo holding several modules has to publish its
# own signed .lgx. This file lists those sources; the "Augment index"
# workflow folds their release assets into index.json after every
# "Rebuild index", so clients see them alongside the locally-built ones.
#
# Format — one entry per line, blank lines and `#` comments ignored:
#
#   owner/repo                 every .lgx asset in that repo's releases
#                              (drafts and the `index` tag are skipped)
#   https://…/foo-1.0.0.lgx    a single package at a fixed URL
#
# `owner/repo` is preferred: new versions published over there are picked
# up automatically, with no edit here.
#
# TRUST: index.py copies whatever signature is inside each .lgx. An
# external package is only trusted by Basecamp if it was signed with a
# key whose DID is listed under `trustedSigners` in logos-repo.json.
# Adding a source here whose signer you don't control means vouching for
# that key — don't.

# Lotion — monorepo publishing `lotion` (core) + `lotion_ui` (QML),
# signed with the same key as this catalog.
hackyguru/lotion

# Workshop series — each part split out of hackyguru/logos-workshop into
# its own repo, publishing its own signed .lgx. Same key throughout.
hackyguru/logos-hello-world
